[Unit]
Description=An app to say Hello World.

[Service]
Type=simple
User="${USER}"
WorkingDirectory=%h
ExecStartPre=npm install %h
ExecStart=npm start %h
# optional items below
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target